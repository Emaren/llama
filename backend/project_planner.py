"""Helps LLaMA break down projects into smaller, trackable tasks."""

def generate_project_plan(project_name):
    return [f"Define goals for {project_name}", f"Break down into steps", f"Estimate time and resources"]
